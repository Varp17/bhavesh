<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LOginForm" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="titleTextColor">@color/white</item>
        <item name="android:statusBarColor">#00F</item>
        <item name="colorPrimaryDark">#049DB5</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.LOginForm" parent="Base.Theme.LOginForm" />
    <!-- -->
    <!-- Light Mode -->
    <!-- Screen background if light mode on  -->
    <style name="SCREEN">
        <item name="android:background">#FFF</item>
    </style>
    <!--texts background if light mode on -->
    <style name="TEXT">
        <item name="android:textColor">#000</item>
    </style>

    <!-- button background if light is on -->
    <style name="BUTTON">
        <item name="android:background">@drawable/teacherlogin</item>
    </style>

    <style name="EDITTEXT" parent="Base.Theme.LOginForm">
        <item name="android:textColor">#000</item>
        <item name="android:background">@drawable/teacheredittext</item>
    </style>
    <style name="edittextLoginblue">
        <item name="android:textColor" Parent="Theme.AppCompat.DayNight.NoActionBar">#D7000000</item>
        <item name="android:background" Parent="Theme.AppCompat.DayNight.NoActionBar">@drawable/edittextloginblueday</item>
    </style>
    <style name="INAPPTEXT" parent="Base.Theme.LOginForm">
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:textColorHint">#AD92EA</item>
        <item name="android:background">@drawable/inapptextnight</item>
    </style>

    <style name="CustomButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:spinnerStyle">?android:spinnerStyle</item>
        <item name="android:textColor">#000</item>
    </style>


    <!--    <style name="logo" parent="Theme.AppCompat.Light.DarkActionBar">-->
    <!--        <item name="android:windowBackground">@drawable/teacher</item>-->
    <!--        <item name="nightModeIcon">@drawable/teacher</item>-->
    <!--    </style>-->



</resources>